name: First Workflow

on: push

jobs:
   build:
      runs-on: ubuntu-latest
      steps:
        - name: print build
          run: echo 'build' 
      
   test:
     runs-on: windows-latest
     
   
   deploy:
     runs-on: ubuntu-latest
     
